@model IEnumerable<TruYum.Models.Cart>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Menuitems.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Menuitems.FreeDelivery)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Menuitems.Price)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Menuitems.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Menuitems.FreeDelivery)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Menuitems.Price)
            </td>
            <td>
                @*@ @Html.ActionLink("Edit", "Edit", new { id = item.CartId }) |
                    @Html.ActionLink("Details", "Details", new { id = item.CartId }) | *@
                @Html.ActionLink("Delete", "Delete", new { id = item.CartId })
            </td>
        </tr>
    }
    @*<div>
            @Html.ActionLink("View MenuItems","MenuItem","MenuItems")
        </div>*@

</table>
<div>
    @Html.ActionLink("View MenuItems", "MenuItem", "MenuItems")
</div>
